<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelManagementSystem"
        tools:targetApi="31">

        <activity android:name=".activities.RevenueActivity" />
        <activity android:name=".activities.AdminBookingActivity" />
        <activity android:name=".activities.AddEditRoomActivity" />
        <activity android:name=".activities.AdminRoomActivity" />
        <activity android:name=".activities.MyBookingsActivity" />
        <activity android:name=".activities.BookingActivity" />
        <activity android:name=".activities.RoomDetailActivity" />
        <activity android:name=".activities.RoomListActivity" />
        <activity android:name=".activities.AdminHomeActivity" />
        <activity android:name=".activities.UserHomeActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.LoginActivity" />

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>